package Arrays_Classes;

public class parallelPrefix {

	public static void main(String[] args) {

//parallelPrefix(originalArray, fromIndex, endIndex, functionalOperator): This method performs parallelPrefix 
		//for the given range of the array with the specified functional operator.

		// parallelPrefix(originalArray, operator): This method performs parallelPrefix
		// for complete array with the specified functional operator.

		// parallelSetAll(originalArray, functionalGenerator): This method set all the
		// elements of this array in parallel, using the provided generator function.
	}

}
